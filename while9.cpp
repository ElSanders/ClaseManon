# include <conio.h>
# include <stdio.h>

int x,y;

main(){
	printf("Introduzca el primer valor: "); scanf("%d",&x);
if(x>0 && x<11){
	printf("Introduzca el segundo valor: "); scanf("%d",&y);
	if(y>0 && x<6){
	}}else{

printf("Error");
getche();}

int c=1;
int p=1;
while (c<=y){
	p=p*x;
	c++;
}
printf("La variable x elevada a la potencia de la variable y es: %d",p);
}
